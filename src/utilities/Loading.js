import React from "react";

const Loading = () => {
	return (
		<div className="w-12 h-12 transition-all border-2 rounded-full border-red dark:border-r-transparent border-r-transparent animate-spin"></div>
	);
};

export default Loading;
